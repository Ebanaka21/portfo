# Vite dev server
VITE_PORT=3000

# Backend used by the Vite proxy
VITE_BACKEND_URL=http://localhost:8080

# Browser API base. Keep /api when frontend and backend share the same host.
VITE_API_BASE_URL=/api
